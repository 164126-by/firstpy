# โปรแกรมคำนวณพื้นที่รูปสี่เหลี่ยมมุมฉาก (เวอร์ชันพัฒนา)

print("=== โปรแกรมคำนวณพื้นที่รูปสี่เหลี่ยมมุมฉาก ===")

while True:
    try:
        กว้าง = float(input("กรุณาใส่ความกว้าง: "))
        ยาว = float(input("กรุณาใส่ความยาว: "))

        พื้นที่ = กว้าง * ยาว
        เส้นรอบรูป = 2 * (กว้าง + ยาว)

        print("\nผลลัพธ์")
        print("พื้นที่ =", พื้นที่, "ตารางหน่วย")
        print("เส้นรอบรูป =", เส้นรอบรูป, "หน่วย")

    except ValueError:
        print("กรุณากรอกตัวเลขเท่านั้น")

    ทำต่อ = input("\nต้องการคำนวณอีกหรือไม่? (y/n): ")
    if ทำต่อ.lower() != "y":
        print("ขอบคุณที่ใช้งานโปรแกรม")
        break
    import math

while True:
    print("\n=== โปรแกรมคำนวณพื้นที่ ===")
    print("1. สี่เหลี่ยมมุมฉาก")
    print("2. วงกลม")
    print("3. สามเหลี่ยม")
    print("4. ออกจากโปรแกรม")

    choice = input("เลือกเมนู: ")

    if choice == "1":
        กว้าง = float(input("ความกว้าง: "))
        ยาว = float(input("ความยาว: "))
        print("พื้นที่ =", กว้าง * ยาว)

    elif choice == "2":
        รัศมี = float(input("รัศมี: "))
        print("พื้นที่ =", math.pi * รัศมี ** 2)

    elif choice == "3":
        ฐาน = float(input("ฐาน: "))
        สูง = float(input("สูง: "))
        print("พื้นที่ =", 0.5 * ฐาน * สูง)

    elif choice == "4":
        print("จบการทำงาน")
        break

    else:
        print("กรุณาเลือกเมนูให้ถูกต้อง")